import 'package:flutter_keycloak/lib.dart';

final FlutterKeycloak _flutterKeycloak = FlutterKeycloak();

/*
void login(String username, String password, {config = "", scope = ""}) async {
  await _flutterKeycloak.login(
    config ? config : defaultKeycloakConfig,
    username,
    password,
    scope: scope ? scope : defaultKeycloakScope,
  );
}
*/